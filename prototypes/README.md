# Prototypes

Standalone, self-contained HTML/CSS/JS explorations — not part of the Next.js app, not built or deployed by it. Each file opens directly in a browser with no build step.

Nothing currently lives here. `box-computer.html` (the retro CRT boot simulation) graduated out of this folder into the real app at `/machine` — see `components/machine/` and `app/machine/page.tsx`.
